# 填报小助手 Agent 技术方案

## 1. 目标说明

本文档用于规划“综测智能体 / 填报小助手”能力建设，面向当前 `Next.js + React + TypeScript + Recoil + Ant Design` 项目，逐步实现以下核心能力：

- 综测问题答疑
- 自助填报辅助
- 成绩查询
- 审批状态查询
- 驳回原因解释与修改建议
- 后续扩展为可执行型 Agent

当前项目已经具备：

- 前端聊天页面基础结构
- 流式输出前端交互
- 基础会话消息结构
- 与现有系统菜单、登录态、页面布局打通

后续需要继续补齐：

- 真实流式问答接口
- 知识库/规则库
- 各类业务查询接口
- Agent 工具调用能力
- 安全、评测、监控能力

---

## 2. 总体建设目标

### 2.1 产品目标

让学生在综合测评填报过程中，可以通过一个统一入口完成：

1. 询问综测规则
2. 确认材料是否合规
3. 判断成果应该归属哪个模块
4. 查询当前成绩情况
5. 查询某条申请的审核进度
6. 理解驳回原因并得到修改建议
7. 后续扩展为直接辅助填写、提交和跳转页面

### 2.2 技术目标

建设一个“前端对话页 + Agent 服务层 + 业务工具层 + 知识检索层”的完整架构，满足：

- 支持流式输出
- 支持多轮上下文
- 支持结构化业务查询
- 支持后续工具调用
- 支持灰度演进，不影响现有系统

---

## 3. 功能范围

### 3.1 第一阶段：问答型 Agent

能力范围：

- 回答综测规则相关问题
- 回答材料要求相关问题
- 回答分类归属相关问题
- 回答通用驳回原因相关问题

不做：

- 不直接写入业务数据
- 不直接替用户提交表单
- 不自动修改系统记录

### 3.2 第二阶段：查询型 Agent

新增能力：

- 查询个人综合测评成绩
- 查询分类得分
- 查询申请记录
- 查询审批状态
- 查询驳回原因

### 3.3 第三阶段：辅助执行型 Agent

新增能力：

- 根据用户问题推荐对应填报模块
- 生成填写建议
- 自动带入部分表单字段
- 联动页面跳转
- 调用业务接口完成部分操作

### 3.4 第四阶段：可执行型 Agent

新增能力：

- 工具调用编排
- 自助填报草稿生成
- 审核前校验
- 失败重试与流程闭环

---

## 4. 技术架构

### 4.1 架构分层

建议采用以下 5 层结构：

1. 展示层
2. 会话编排层
3. Agent 能力层
4. 业务工具层
5. 知识与数据层

### 4.2 分层说明

#### 4.2.1 展示层

位置建议：

- `src/app/(dashboard)/comprehensiveAgent/fillAssistant/page.tsx`

职责：

- 聊天消息展示
- 用户输入
- 流式输出渲染
- 停止生成
- 会话交互反馈

#### 4.2.2 会话编排层

位置建议：

- `src/services/api/assistant.ts`
- 后续可拆出 `src/services/assistant/`

职责：

- 管理消息历史
- 封装流式请求
- 处理分块数据
- 控制中断
- 统一错误处理

#### 4.2.3 Agent 能力层

建议后端建设独立能力：

- `chat`
- `route`
- `tool calling`
- `knowledge retrieval`

职责：

- 判断用户意图
- 决定是直接回答还是调用工具
- 组织上下文
- 生成最终答案

#### 4.2.4 业务工具层

职责：

- 查询成绩
- 查询审批状态
- 查询申请记录
- 查询驳回原因
- 后续提交草稿、辅助填报

这一层建议和普通业务 API 解耦，Agent 只通过工具层访问业务系统。

#### 4.2.5 知识与数据层

包括：

- 综测规则文档
- 学院细则
- FAQ
- 业务数据库
- 用户历史填报记录

---

## 5. 前端设计方案

### 5.1 页面职责

填报小助手页面主要负责：

- 消息展示
- 流式输出
- 推荐问题点击发送
- 停止生成
- 会话记录保留

### 5.2 当前前端可复用能力

当前项目已经具备：

- 固定输入栏
- 对话列表渲染
- 流式分块追加
- 推荐问题快捷发送
- 停止生成按钮

### 5.3 前端建议继续扩展的能力

#### 5.3.1 消息类型扩展

当前消息结构：

```ts
type ChatMessage = {
  role: "user" | "assistant";
  content: string;
};
```

建议升级为：

```ts
type ChatMessage = {
  id: string;
  role: "user" | "assistant" | "system" | "tool";
  content: string;
  type?: "text" | "status" | "tool_result" | "card";
  createdAt?: string;
  meta?: Record<string, unknown>;
};
```

#### 5.3.2 会话状态管理

建议增加：

- 当前会话 ID
- 当前学年
- 当前上下文模块
- 是否正在调用工具
- 是否正在流式输出
- 最近一次错误

Recoil 建议新增：

- `assistantSessionState`
- `assistantMessagesState`
- `assistantContextState`

#### 5.3.3 UI 组件拆分

建议后续拆成：

- `ChatHeader`
- `PromptSuggestions`
- `MessageList`
- `MessageBubble`
- `Composer`
- `StreamingIndicator`

---

## 6. 后端接口方案

### 6.1 总体原则

为了支持流式问答与工具调用，建议 Agent 接口不要直接复用原有普通业务接口，而是新建一组 Agent 专用接口。

### 6.2 建议接口清单

#### 6.2.1 流式聊天接口

`POST /assistant/streamChat`

请求体建议：

```json
{
  "question": "德育材料需要上传什么？",
  "history": [
    { "role": "user", "content": "..." },
    { "role": "assistant", "content": "..." }
  ],
  "context": {
    "academicYear": "2025-2026",
    "module": "moralEducation"
  }
}
```

返回建议：

- `SSE`
- 或 `chunked response`

返回分块建议：

```text
data: 你好
data: ，德育材料通常包括
data: ...
data: [DONE]
```

#### 6.2.2 查询成绩接口

`POST /assistant/queryScore`

职责：

- 查询用户当前综合测评成绩
- 按学年查询
- 按模块查询

#### 6.2.3 查询审批状态接口

`POST /assistant/queryApprovalStatus`

职责：

- 查询某一条申请的审批进度
- 查询全部申请状态
- 查询被驳回记录

#### 6.2.4 查询申请记录接口

`POST /assistant/queryApplicationRecords`

职责：

- 按学年查询
- 按模块查询
- 按状态查询

#### 6.2.5 填报建议接口

`POST /assistant/suggestFilling`

职责：

- 识别成果归类
- 判断应填入哪个模块
- 返回需要填写的字段和材料要求

#### 6.2.6 驳回解释接口

`POST /assistant/explainRejection`

职责：

- 结合申请记录和驳回原因
- 生成可执行修改建议

---

## 7. 知识库方案

### 7.1 需要纳入知识库的内容

- 综合测评总则
- 学院实施细则
- 德育/智育/体育美育/劳育评分规则
- 材料要求说明
- 驳回原因 FAQ
- 学年选择规则
- 典型问题示例

### 7.2 知识形态建议

优先级从高到低：

1. 结构化 FAQ
2. 规则条款文档
3. 可检索知识片段
4. 历史高频问题

### 7.3 第一阶段知识库实现建议

最简单可落地方案：

- 先整理成 Markdown / JSON FAQ
- 后端做关键词检索 + Prompt 拼接
- 暂不急着上复杂 RAG

### 7.4 第二阶段知识库升级建议

升级为：

- 向量检索
- 片段召回
- 多文档融合
- 条款引用

---

## 8. Agent 工作流设计

### 8.1 问题答疑流程

1. 用户输入问题
2. Agent 判断是否为规则问答
3. 检索知识库
4. 拼接上下文
5. 流式返回回答

### 8.2 成绩查询流程

1. 用户输入“帮我看下本学年综测成绩”
2. Agent 识别意图为成绩查询
3. 调用查询工具
4. 获取结构化结果
5. 生成自然语言回答
6. 流式返回

### 8.3 审批状态查询流程

1. 用户输入“我上次德育申请审核到哪了”
2. Agent 判断为审批状态查询
3. 提取模块、学年、申请范围
4. 调用状态查询工具
5. 生成状态说明
6. 返回结果

### 8.4 自助填报辅助流程

1. 用户描述成果
2. Agent 判断成果类型
3. 匹配模块
4. 返回填报建议
5. 可联动打开对应填写弹窗

---

## 9. Prompt 设计建议

### 9.1 System Prompt 目标

需要约束助手：

- 只回答综测相关问题
- 不杜撰制度和分值
- 不确定时明确说明
- 优先给出可执行建议
- 查询类问题必须以接口结果为准

### 9.2 System Prompt 建议内容

- 角色定义：综测填报助手
- 能力边界：问答、查询、建议，不替代最终审核
- 回答风格：清晰、简洁、步骤化
- 风险限制：不伪造审批结果，不伪造成绩

### 9.3 输出格式建议

对用户尽量返回：

1. 结论
2. 原因
3. 下一步建议

例如：

```text
你这项内容更适合填在智育模块。
原因是它属于学术成果类内容，且需要论文或专利证明。
建议你下一步准备成果证明、作者信息和附件材料后再提交。
```

---

## 10. 数据与状态设计

### 10.1 会话上下文

建议维护以下上下文：

- 当前用户
- 当前学年
- 当前测评模块
- 最近查询过的申请记录
- 最近查询过的成绩信息
- 最近一次驳回原因

### 10.2 前端消息存储

建议：

- 当前会话存在 Recoil
- 可选持久化到 `localStorage`
- 后续支持多会话切换

### 10.3 后端会话存储

建议：

- 按用户存储短期会话上下文
- 保留最近 N 轮
- 为 Agent 工具调用提供连续语义

---

## 11. 安全与权限设计

### 11.1 权限控制

必须保证：

- 只能查询当前登录用户自己的成绩和审批数据
- 不能越权查询其他学生信息
- 不能通过对话接口绕过原业务权限

### 11.2 数据脱敏

涉及敏感数据时建议：

- 对学号做脱敏
- 对班级信息按需显示
- 避免暴露无关审批人信息

### 11.3 操作留痕

如果后续支持可执行操作，必须记录：

- 用户 ID
- 会话 ID
- 调用时间
- 调用工具
- 参数摘要
- 执行结果

---

## 12. 开发里程碑

### 12.1 M1：流式问答 MVP

时间建议：1-2 天

内容：

- 前端聊天页稳定
- 流式接口联调
- 本地 fallback 问答
- 推荐问题快捷发送

### 12.2 M2：规则知识问答

时间建议：3-5 天

内容：

- 规则 FAQ 整理
- 知识库接入
- 常见问题命中优化

### 12.3 M3：成绩与状态查询

时间建议：5-7 天

内容：

- 成绩查询工具
- 审批状态查询工具
- 申请记录查询工具

### 12.4 M4：自助填报辅助

时间建议：7-10 天

内容：

- 归类建议
- 材料清单生成
- 表单联动建议

### 12.5 M5：可执行型 Agent

时间建议：10-15 天

内容：

- 工具调用编排
- 草稿写入
- 页面跳转联动

---

## 13. 模块拆分建议

建议目录逐步演进为：

```text
src/
  app/
    (dashboard)/
      comprehensiveAgent/
        fillAssistant/
          page.tsx
  components/
    assistant/
      ChatHeader.tsx
      PromptSuggestions.tsx
      MessageList.tsx
      MessageBubble.tsx
      Composer.tsx
  services/
    api/
      assistant.ts
    assistant/
      stream.ts
      parser.ts
      session.ts
      tools.ts
  store/
    atom/
      assistant.ts
```

---

## 14. 当前项目建议立即执行的事项

### 14.1 前端侧

- 将当前聊天页拆组件
- 增加会话状态 atom
- 增加错误态和重试态
- 增加空状态和欢迎态

### 14.2 接口侧

- 确认真实流式接口地址
- 统一流式协议格式
- 明确查询成绩/审批状态接口清单

### 14.3 知识库侧

- 收集综测规则文档
- 收集驳回原因案例
- 收集材料要求说明

### 14.4 Agent 侧

- 先做规则问答
- 再做查询工具
- 最后做填报辅助

---

## 15. 风险与注意事项

### 15.1 规则准确性风险

如果知识库不完整，Agent 容易回答不准确。

解决建议：

- 所有制度类内容必须来源于已确认文档
- 不确定时明确提示“请以学院最新要求为准”

### 15.2 查询结果可信度风险

如果 Agent 混淆了“模型生成”和“接口返回”，会误导用户。

解决建议：

- 查询类结果必须优先展示真实接口结果
- 模型只负责解释，不负责伪造数据

### 15.3 多轮上下文漂移风险

连续问答后，模块和学年可能识别错乱。

解决建议：

- 显式维护上下文
- 查询前重复确认关键参数

### 15.4 可执行操作风险

如果后续支持自动填报或提交，风险会更高。

解决建议：

- 默认生成建议，不直接提交
- 关键操作必须二次确认

---

## 16. 结论

填报小助手 Agent 的建设建议分四步走：

1. 先完成流式问答能力
2. 再接入规则知识库
3. 再补充成绩与审批状态查询
4. 最后扩展成可执行型 Agent

对于你当前项目来说，最优策略不是一开始就做“全自动 Agent”，而是先做：

- 好用的对话页面
- 稳定的流式输出
- 可靠的知识问答
- 可验证的业务查询

等这些能力稳定后，再进入自助填报和工具调用阶段。
